name: "Метки: PR"

on:
- pull_request_target

jobs:
  labeler:
    name: Расстановка меток
    if: github.actor != 'IanComradeBot'
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v5
